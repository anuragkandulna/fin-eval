[pytest]
testpaths = tests
addopts = -v --tb=short --html=reports/eval_report.html --self-contained-html
asyncio_mode = auto
markers =
    eval: deepeval LLM quality tests
    rag: RAG faithfulness and contextual relevancy tests
    hallucination: adversarial hallucination trap tests
